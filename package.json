{
  "name": "infinity-provider-pi",
  "version": "0.1.0",
  "description": "Infinity model provider process that proxies to pi built-in and installed model providers.",
  "type": "module",
  "bin": {
    "infinity-provider-pi": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build --silent && node ./dist/main.js",
    "check": "tsc --noEmit",
    "prepare": "npm run build",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.75.5",
    "@earendil-works/pi-coding-agent": "^0.75.4"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
